# Public framework: resolve from the public npm registry over HTTPS so the build is
# reproducible for anyone. (Overrides any private-registry pin from a global .npmrc.)
registry=https://registry.npmjs.org/
